import {FC} from "react";
import {Typography} from "@material-ui/core";


const FilterPanel: FC = () => {
  return <Typography variant="body1">Filter results</Typography>;
};

export default FilterPanel;
