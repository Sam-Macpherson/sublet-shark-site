/**
 * The styles for the RecoverPassword page.
 */

import { makeStyles } from "@material-ui/core/styles";


const useStyles = makeStyles(theme => ({
  gridItem: {
    padding: theme.spacing(2),
  },
}));


export { useStyles };